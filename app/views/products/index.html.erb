<% provide(:title, (@category ? @category.name : '') ) %>
<div class="row">
  <%= render "left_menu" %>
  <div class="col-md-9 border_left">
  	<h3 class="text-left"><%= @category ? @category.name : 'News Items' %></h3>
  	<%= will_paginate %>
    <%=  render partial: "product_row", collection: @products, as: :product, locals: { order_item: @order_item } %>
    <%= will_paginate %>
  </div>
</div>
